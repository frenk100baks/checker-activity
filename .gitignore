# Databases (contain sensitive tokens and credentials)
*.db.json
db.json

# Local machine paths / runtime config
config.json

# Environment and credentials
.env
.env.*
*.pem
*.key
credentials.json
.claude/